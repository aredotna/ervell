case block.get('class')
  when "User"
    include types/user
  when "Channel"
    include types/channel
  when "Text"
  when "Image"
  when "Link"
  when "Media"
  when "Attachment"
    include types/block
  when "Block"
    include types/loading
  default
    include types/placeholder
