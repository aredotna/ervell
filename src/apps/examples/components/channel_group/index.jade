.ChannelBlockGroup.js-channel-group(data-id=channel.id)
  .ChannelBlockGroup__header
    .ChannelBlockGroup__title
      h4.Type.Type--size-h4
        a(href=channel.href)
          | Are.na / #{channel.user.name} /&nbsp;
          span(class='Color--channel-#{channel.visibility}')!= channel.title
    .ChannelBlockGroup__follow.js-example-channel-follow(data-id=example.id)
      //- rendered on the client
  .ChannelBlockGroup__inner.js-channel(data-href=channel.href)
    if channel.blocks
      for block in channel.blocks
        include ../../../../components/block_v2/templates/block

