extends ../../../components/path/templates/path.jade

block path
  li.path__username
    a(href="/" + author.get('slug'))=author.get('username')
  li.path__sub: span Manage channels

block right
  if author && user && author.id == user.id
    .channel-settings.edit-profile
      a(href="#") Profile settings
      br
      a(href="/manage") Manage channels