doctype html
html(class=sd.IS_INVERTED ? 'is-inverted' : '')
  head
    title
      | Are.na
      if channel
        |  / #{channel.get('title')}

    link( type='text/css', rel='stylesheet', href=asset('/assets/embed.css') )
  body
    .container.channel-embed
      include _embed-header
      include ../../../components/blocks/container/templates/index
    include ../../../components/layout/analytics

    #scripts
      != sharify.script()
      script( src="//ajax.googleapis.com/ajax/libs/jquery/2.1.0/jquery.min.js" )
      script( src=asset('/assets/embed.js') )
