.feed-container
  padding-bottom 200px
  position relative
  height auto

.metadata--selector--notifications.has-notifications
  .notifications--count
    color alert
    padding-left 3px

    &:before
      color lightgray
      content "("

    &:after
      color lightgray
      content ")"