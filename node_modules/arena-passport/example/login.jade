mixin email-password
  input( name='email', placeholder='Email...', type='email' )
  input( name='password', placeholder='Password...', type='password' )

doctype html
html(lang="en")
  head
    title Login
  body
    h1 Login
    form( action='/me/sign_in', method='POST' )
      mixin email-password
      button( type='submit' ) Login
    hr