#auth-page.vam-outer
  .vam-inner
    #avatar
      //- rendered client side
    section#auth-body
      form.auth-login-settings.auth-form( action='/me/sign_in' )
        .form__field
          label First name
          input.input--wide.input--relative.is-block(
            name='first_name' type='text' value="#{user.get('first_name')}" placeholder='First name' required autofocus= autofocus
          )
        .form__field
          label Last name
          input.input--wide.input--relative.is-block(
            name='last_name' type='text' value="#{user.get('last_name')}" placeholder='Last name' required autofocus= autofocus
          )
        .form__field
          label Email
          input.input--wide.input--relative.is-block(
            name='email' type='email' value="#{user.get('email')}" placeholder='Email' required autofocus= autofocus
          )
        .form__field
          label Password
          input.input--wide.input--relative.is-block(
            name='password' type='password' placeholder='Password'
          )
        .form__field
          label Repeat password
          input.input--wide.input--relative.is-block(
            name='password_confirmation' type='password' placeholder='Repeat password'
          )
        .auth-errors &nbsp;

        button#auth-settings.auth-submit.button--wide.button--border.button--relative Save