header#layout-header
  .layout-header__search
    input#layout-header__search__input(type="text", placeholder="Search Arena")
    .layout-header__search__close.pointer &times;
    .layout-header__search__results
      //- rendered by client

  if user && user.has('username')
    .user.dropdown--menu.right
      .username.dropdown--menu__trigger **
      .dropdown.dropdown--desktop-size
        a.dropdown__link(href="/#{user.get('slug')}")= user.get('username') 
          [avatar]
        a.dropdown__link(href="#") Tools
        a.dropdown__link(href="#") About & FAQ
        a.dropdown__link(href="#") Settings
        a.dropdown__link(href="/me/sign_out") Sign Out

  else
    .register
      span
        a(href="#log_in").btn-login Login 
      span or 
      span
        a(href="#log_in").btn-signup Sign Up
 //-
      .logo.dropdown--menu.right
        .logo-icon.dropdown--menu__trigger
          include ../../../../public/images/arena-mark.svg