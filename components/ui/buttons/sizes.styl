font = json('../typography/font.json', { hash: true })

.Button
  &--compact
    padding input-padding

  for key, size in font.font
    &--{key}
      font-size size
      if size >= font.font['size-lg']
        border-width 2px
